# =============================================================================
# Search Service Production Requirements
# 
# Minimal requirements for the search service.
# Includes packages that are imported at module level by app.api.v1.notes
# but EXCLUDES heavy ML packages (sentence-transformers, docling, etc.)
# =============================================================================

# FastAPI and serving
fastapi==0.137.0
uvicorn[standard]==0.49.0
python-multipart==0.0.32
pydantic-settings>=2.14.1
pydantic>=2.0.0

# HTTP client
httpx>=0.28.1

# Environment
python-dotenv>=1.2.2

# OpenAI SDK (imported by embeddings.py even though we override it)
openai>=1.45.0

# Supabase (notes_db.py)
supabase>=2.31.0

# Azure Storage (blob_storage.py)
azure-storage-blob>=12.30.0

# Groq (for chat endpoints)
groq>=0.9.0

# LangChain/LangGraph (for chat_agent.py)
langchain>=0.1.0
langchain-core>=0.1.0
langchain-groq>=0.1.0
langgraph>=0.2.0

# JWT/Auth
python-jose[cryptography]>=3.3.0

# Basic data processing
numpy>=1.24.0

# =============================================================================
# EXCLUDED (heavy packages not needed for production search service):
# - sentence-transformers: Worker handles embeddings
# - docling: Upload service handles document processing
# - ragas: Evaluation only, not runtime
# - faiss-cpu: Worker handles vector search
# - langgraph: Not used in search routes
# - datasets: Evaluation only
# =============================================================================
