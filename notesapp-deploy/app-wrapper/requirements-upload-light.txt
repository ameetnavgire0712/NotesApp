# =============================================================================
# Upload Service - Lightweight dependencies (uses Worker for embeddings)
# =============================================================================

# Web framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# HTTP client (for Worker API calls)
httpx>=0.26.0
tenacity>=8.2.0
aiohttp>=3.9.0

# Supabase
supabase>=2.3.0

# Utilities
python-dotenv>=1.0.0
python-multipart>=0.0.6
python-jose[cryptography]>=3.3.0

# Azure Blob Storage
azure-storage-blob>=12.19.0

# TensorLake (document parsing)
tensorlake>=0.1.0

# Title generation (uses Groq - lightweight)
groq>=0.4.0

# Data processing
numpy>=1.24.0

# OpenAI SDK (imported by embeddings.py for interface)
openai>=1.45.0

# =============================================================================
# EXCLUDED (Worker handles embeddings):
# - sentence-transformers
# - torch
# - docling (using TensorLake instead)
# =============================================================================
