{
  "name": "notesapp-vector-search",
  "version": "1.0.0",
  "description": "Cloudflare Worker for NotesApp vector search at the edge",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240129.0",
    "typescript": "^6.0.3",
    "wrangler": "^4.61.1"
  }
}
